export interface RegisterUserData {
    client_id: string;
    client_secret: string;
    name: string;
    email: string;
    password: string;
}

export interface LoginUserData {
    client_id: string;
    client_secret: string;
    email: string;
    password: string;
}